---
name: Common UI properties
---

## Color

## Size

## Positions

We use `start` and `end` instead of `left` and `right` to avoid confusions with RTL layout.

#### LTR vs RTL

<img src={require("public/images/CommonProperties/props-position.png")} />
